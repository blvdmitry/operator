import React from "react";
import type * as T from "./Grid.types";
declare const Grid: {
    (props: T.Props): React.JSX.Element;
    Column: (props: T.ColumnProps) => React.JSX.Element;
};
export default Grid;
