/**
 * The properties that don't have color scales assinged use the background
 * color that is passed
 */
export declare const colorsToGenerate: Record<string, Record<string, {
    shade?: number;
    tint?: number;
    alpha?: number;
}>>;
